This is a simple JavaScript based calculator capable of addition(+), subtraction(-), multiplication(*), and division(/).
To use simply download the project directory and run the html file.
The calculator part is laid out in a way reminiscent writing it out by hand, simply enter 2 numbers select the desired operation and click the "Calculate" button.
